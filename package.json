{
  "name": "instagram-auto",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace frontend && cargo build",
    "dev:backend": "cargo run -p instagram-auto-backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "lint": "npm run lint --workspace frontend",
    "format": "npm run format --workspace frontend && cargo fmt --all"
  },
  "workspaces": [
    "frontend"
  ]
}
